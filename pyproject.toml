[project]
name = "foreman-chatbot"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12.0"
dependencies = [
            "slackclient",
            "python-dotenv",
            "pyyaml",
            "jsonpath-rw",
            "requests",
            "sentry-sdk",
]
